# action.yml
name: 'Export Namespace'
description: 'Export a namespace.'
inputs: 
  openshift-namespace:  
    description: 'Namespace'
    required: true 
runs:
  using: "composite"
  steps:
    - run: "bash ./scripts/export-current-ns.sh"
      shell: bash 
 
